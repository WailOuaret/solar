# Dataset Cards

This directory stores dataset cards and manifests generated by `scripts/download_data.py`.

Expected files:

- `deepsolareye_dataset_card.yaml`
- `villegas_dataset_card.yaml`
- `trsai_dataset_card.yaml`
- corresponding manifest files with checksums for any raw files already present
